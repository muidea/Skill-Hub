# skill-hub v0.10.1

## 问题修复

- 移除无实际消费者的 Skill `compatibility` 数据模型，避免过期工具适配信息进入技能索引、API 响应和创建模板。
- `list` 不再展示“适用范围”列，Web 技能目录不再展示该字段。
- 移除对应的解析、校验、自动转换和注册表写入逻辑，避免后续更新再次产生无效字段。

## 技能与文档

- 刷新内置 workflow skills、示例、安装说明、设计文档和命令规范，统一采用无工具适配范围的技能元数据。
- 更新单元测试与端到端用例，覆盖无目标元数据的列表、搜索和服务响应。
