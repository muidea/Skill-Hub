# v0.13.3 嵌套项目工作区边界

## 修复

- 当前目录存在 `.agents/skills/` 时，Skill-Hub 将其识别为独立项目工作区，不再错误继承父目录的项目状态。
- 首次执行项目命令时沿用现有确认流程创建当前目录的状态记录；已有技能文件不会被覆盖，也不会被自动登记。
- 没有 `.agents/skills/` 标记的普通子目录继续继承最近的父项目状态。

## 验证

- `make lint`
- `go test ./... --count=1`
- `make build`
